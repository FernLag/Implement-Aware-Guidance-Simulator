"""Web interface for the implement-aware guidance simulator.

Kept strictly separate from the simulation core. `aggsim` has no knowledge of
this package, so Stages 0-6 remain runnable with NumPy, Matplotlib and PyYAML
alone, as the project constraints require.
"""
