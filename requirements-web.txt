# Web interface only. The simulation core in aggsim/ needs none of this and
# still runs with NumPy, Matplotlib and PyYAML alone, as the project
# constraints require.
#
# Pin these before deploying, then scan the pinned set:
#   pip-audit -r requirements-web.txt

Flask>=3.0,<4
pydantic>=2.5,<3
Jinja2>=3.1,<4

# Production server. The Flask development server must not face the internet.
gunicorn>=21.2 ; sys_platform != "win32"

# Supplies a root certificate bundle on installs that ship without one, which
# is the case on python.org macOS builds. Used to VERIFY TLS to the USGS
# services, never to skip verification.
certifi>=2024.2.2
